version: 2

jobs:
  job_to_trigger:
    machine: true
    steps:
      - run:
          command: |
            if [[ ${IS_BUILD_APPS_FOR_PR_LABEL} ]]; then
              echo "LABEL"
            fi
  job_regular:
    machine: true
    steps:
      - run: echo "No Label"

workflows:
  version: 2
  build-test-deploy:
    jobs:
      - job_to_trigger
      - job_regular
